{
  "name": "continuum",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CONTINUUM: multi-agent development runtime — Phase 3 provider-adapter foundation.",
  "bin": {
    "continuum": "./dist/cli/bin.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "js-tiktoken": "^1.0.21"
  }
}
