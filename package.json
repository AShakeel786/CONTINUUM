{
  "name": "continuum",
  "version": "0.1.0-beta.1",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "CONTINUUM: multi-agent development runtime — Claude Code, DeepSeek, Codex, and any OpenAI/Anthropic-compatible API through one provider/tool layer.",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "continuum": "./dist/cli/bin.js",
    "continuum-mcp": "./dist/mcp/bin.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "js-tiktoken": "^1.0.21"
  }
}
